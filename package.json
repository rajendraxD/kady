{
  "name": "kady",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run seed --prefix backend && npm run dev --prefix backend && npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "dev": "npm run seed --prefix backend && npm run dev --prefix backend && npm run dev --prefix frontend",
    "clean": "rimraf node_modules frontend/node_modules backend/node_modules",
    "install:all": "npm install && npm install --prefix frontend && npm install --prefix backend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "concurrently": "^10.0.3",
    "rimraf": "^6.1.3"
  }
}
